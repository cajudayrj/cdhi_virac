$(document).ready(function () {
    var myPhotoSwipe = $(".service-gallery img").photoSwipe({
        enableMouseWheel: false,
        enableKeyboard: false

        // add configuration options here

    });
});